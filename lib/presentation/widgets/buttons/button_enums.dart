enum ButtonSize {
  small, // 32px height
  medium, // 40px height
  large, // 48px height
}

enum ButtonClassType {
  standard, // Primary, e.g., blue
  dangerous, // Destructive, e.g., red
}

enum IconButtonShape {
  circle,
  square,
}

enum IconButtonType {
  filled,
  outline,
} 